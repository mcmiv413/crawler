/**
 * DEPRECATED: Re-export shim for viewport constants.
 * All constants are now in apps/web/src/config/ui-config.ts
 * This file exists only for backward compatibility.
 */

export {
  VP_WIDTH,
  VP_HEIGHT,
  CELL_SIZE,
  VIEWPORT_PX_WIDTH,
  VIEWPORT_PX_HEIGHT,
} from '../config/ui-config.js';
