# Optional local AI configuration.
# pnpm scripts do not load this file automatically; export these values in your shell
# before running dev or balance commands if you want LM Studio instead of fallback-only mode.
LM_HOST=127.0.0.1
LM_PORT=1234
